@{
	var controller = ViewData["Controller"] as string;
	var area = ViewData["Area"] as string;
}

<ul class="navbar-nav me-auto mb-2 mb-lg-0 flex-row gap-3">
	<li class="nav-item">
		<a class="nav-link @(controller == "home" ? "active" : "")" aria-current="page" asp-controller="Home" asp-action="Index">Lab1</a>
	</li>
	<li class="nav-item">
		<a class="nav-link @(controller == "product" ? "active" : "")" asp-controller="Product" asp-action="Index">Каталог</a>
	</li>
	<li class="nav-item">
		<a class="nav-link @(area == "admin" ? "active" : "")" asp-area="Admin" asp-page="/Index">Администрирование</a>
	</li>
</ul>